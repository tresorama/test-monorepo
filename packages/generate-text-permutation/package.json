{
  "private": false,
  "name": "@tresorama-test-lib/generate-text-permutation",
  "version": "1.0.0",
  "author": "Jacopo Marrone",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "compile": "tsup src/index.ts --format cjs --dts",
    "dev": "yarn compile -- --watch",
    "build": "yarn clean && yarn compile",
    "prepack": "yarn build && cp \"./package.json\" ./dist/",
    "clean": "rimraf ./dist"
  },
  "devDependencies": {
    "shared-tsconfig": "*",
    "@types/node": "^16.18.3",
    "rimraf": "^3.0.2",
    "tsup": "^6.3.0",
    "typescript": "^4.8.4"
  }
}
