{
  "private": true,
  "name": "tresorama-test-monorepo",
  "version": "0.0.0",
  "author": "Jacopo Marrone <jacopo.marrone27@gmail.com>",
  "license": "MIT",
  "engines": {
    "npm": "*"
  },
  "workspaces": [
    "apps/**",
    "packages/**"
  ],
  "devDependencies": {
    "@types/node": "^16.18.3",
    "lerna": "^6.0.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --stream",
    "clean": "lerna run clean --stream",
    "preinstall": "npx only-allow npm"
  },
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
